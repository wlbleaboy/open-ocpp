# OCPP 2.1 examples

This directory contains a minimal OCPP 2.1 charge point example.

The first example starts an OCPP 2.1 charge point stack and forwards all CSMS requests to a default handler that replies with `NotSupported`. It is intended as a starting point for adding OCPP 2.1 business behavior.
